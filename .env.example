# Database
DATABASE_URL="file:./dev.db"

# Admin credentials
ADMIN_USER="admin"
ADMIN_PASS="changeme123"

# SMTP Settings (optional - will log to console if not set)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="user@example.com"
SMTP_PASS="password"
SMTP_FROM="noreply@mediacareers.in"

# Email recipient for applications
APPLICATION_EMAIL="info@phildass.com"

# Session secret (generate a random string)
SESSION_SECRET="change-this-to-a-random-secret-string-in-production"

# Next.js
NEXT_PUBLIC_APP_URL="http://localhost:3000"
